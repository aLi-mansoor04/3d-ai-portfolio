# Optional: wire a real LLM into "Ask My Portfolio" later.
# Never call an LLM API directly from the browser with a key exposed via
# VITE_* — those are bundled into client JS and publicly visible. Instead,
# add a serverless/API route (Vercel/Netlify function, etc.) that reads this
# key server-side and swap the body of answer() in src/lib/knowledgeBase.ts
# to fetch() that route instead of doing local keyword matching.

LLM_API_KEY=
